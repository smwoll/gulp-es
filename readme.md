# Gulp + esbuild basic starter

Simple project to set up and test gulp + esbuild, with sass support.

## Install

```bash
npm i esbuild esbuild-plugin-sass
```

## Gulp Commands

```bash
gulp build
```

```bash
gulp watch
```
